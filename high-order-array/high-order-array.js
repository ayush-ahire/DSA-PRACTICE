const arr = [1, 2, 3, 4, 5];

// mapping an array
const numbers1 = arr.map((nums) => {
  return nums * 2;
});

console.log(numbers1);

// const numbers2 = arr.filter((nums) => {
//   return nums % 2 == 0;
// });
// console.log(numbers);

// const numbers = arr.reduce((total, num) => {
//   return (total = total + num * 2);
// }, 0);

// console.log(numbers);

// const findNumber = arr.find((num) => num > 4);
// console.log(findNumber);

// const allNumbers = arr.every((num) => num > 0);
// console.log(allNumbers);
